// 为限制元素类型和数量的数组
let arr: [string, number] = ['lin', 22]

// 应用场景，定义二维数组的字段类型
let excel: [string, string, number][] = [
  ['title', 'name', 3]
]

let excel1: Array<[string, string, number]> = [
  ['title', 'name', 3]
]